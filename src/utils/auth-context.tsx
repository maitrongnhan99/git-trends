"use client";

// Re-export the auth context from the new location
export { AuthProvider, useAuth } from "@/lib/auth/auth-context";

// The old implementation has been moved to src/lib/auth/auth-context.tsx
